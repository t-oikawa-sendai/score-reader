# SKILL: 正確に楽譜を読む（MusicXML 検証ツール）

## 目的
画像/PDFの楽譜をOMR(光学楽譜認識)でMusicXML化した後、その構造化データを
**推測せず、書いてある通りに**読み、誤りが混入しやすい箇所を機械的に検査する。

## 役割分担（重要）
- 画像→MusicXMLの変換は **このツールの責務外**。外部OMR(例: Audiveris)で行う。
- このツールは **変換後のMusicXMLの正確性検証** に専念する。
- 「正確さ」の最終責任はOMR出力品質に依存する。本ツールはその誤りを下流で炙り出す。

## 設計原則（厳守）
1. 推測しない。読めない/曖昧な要素は黙殺せず WARNING として列挙する。
2. 移調楽器は記音(written)と実音(sounding)を必ず区別して報告する。
3. 拍子と各小節の音価合計を検算し、不一致を ANOMALY として検出する。
4. 「異常0件」でも完全無欠を主張しない。元譜との目視照合を必ず促す。

## 技術スタック
- 言語: Python 3.12+
- 主要ライブラリ: music21 (10.3.0で動作確認済み)
  - 導入: `pip install music21`
- 選定理由: MusicXMLパース・移調楽器処理・拍子/調号判定を標準装備。
  自前実装を避けられ、安定性が高い。

## 必須機能（8項目）
| # | 機能 | 検出/出力内容 |
|---|------|--------------|
| 1 | 移調楽器テーブル | 各パートの管調・記音/実音差(半音)を報告。移調情報欠落も警告 |
| 2 | 小節長の検算 | 拍子 × 4 と小節の quarterLength を比較。不一致は ANOMALY。アウフタクトは許容 |
| 3 | 調号の列挙 | 調号と変化点(小節番号)を出力 |
| 4 | テンポ/拍子イベント | 変化点を小節番号順にリスト化 |
| 5 | 未確定要素の列挙 | 無音高要素等を列挙。完全性を主張しない注記を必ず付す |
| 6 | リハーサルマーク対応表 | リハーサルマーク↔小節番号の対応を一覧化。空内容・小節未確定・重複は WARN |
| 7 | 和音の音数チェック | 各和音の構成音数を報告。単音記譜は INFO、パート主流と相違する音数は WARN(音抜け/重複の疑い)。音数の正誤は断定せず元譜照合を促す |
| 8 | パート間の小節数整合 | 各パートの小節数を比較し不一致を WARN。検出可能=パート間の小節数不一致。検出不能=正しいパート・欠落した正確な位置・元譜との完全一致。単一パートは異常扱いしない |

## 実装方針（Cursorへの指示）
- `src/verify_score.py` の雛形(別添)をベースに拡張する。
- 各機能は独立関数とし、単体テスト可能にする。
- CLI: `python3 verify_score.py <input.musicxml>` で全項目を順に実行。
- 出力は ANOMALY / WARN を最優先で目立たせる。

## 使い方
- CLI は argparse で整理。位置引数=入力ファイル、`--json`=出力フラグ。

```
python3 verify_score.py <input.musicxml>          # 人間可読(デフォルト)
python3 verify_score.py <input.musicxml> --json   # 構造化JSONを標準出力
```

- `--json` 指定時は検証[1]〜[7]の結果を構造化JSONで標準出力する。
  人間可読出力は維持され、フラグ未指定がデフォルト。
- WARN/ANOMALY は各セクションのデータに加え `warnings` 配列にも集約し、
  機械処理(教材システム連携等)で拾えるようにする。

### JSON出力のキー構成
| キー | 内容 |
|------|------|
| `parts` | パート数(int) |
| `transposing_instruments` | [1] 各パートの移調情報(管調・半音差) |
| `measure_anomalies` | [2] 小節長検算の WARN/ANOMALY 一覧 |
| `key_signatures` | [3] 調号と変化点(小節番号) |
| `tempo_meter_events` | [4] テンポ/拍子イベント(小節番号順) |
| `rehearsal_marks` | [6] リハーサルマーク↔小節番号の対応 |
| `chord_voicing` | [7] パート別の和音音数・分布・各和音のフラグ |
| `part_measure_counts` | [8] パート別小節数・全パート一致の真偽・不一致時の警告 |
| `unreadable` | [5] 未確定要素(無音高数)と完全性非主張の注記 |
| `warnings` | WARN/ANOMALY の横断集約(section/level/part/measure/message) |
| `note` | 音高の正誤は判定不可・元譜目視照合必須の注記 |

## 実装済み（旧・拡張候補より昇格）
1. リハーサルマーク↔小節番号の対応表(吹奏楽スコアで頻出) → 検証[6]
2. 和音の音数チェック(OMRの音抜け検出) → 検証[7]
3. JSON出力モード(教材システムへの連携用) → `--json`
4. 複数パート間の小節数整合チェック → 検証[8]
   - 検出可能: パート間の小節数不一致
   - 検出不能: 正しいパート・欠落した正確な位置・元譜との完全一致
   - 「異常0件でも完全無欠を主張しない」原則を維持

## 拡張候補（優先度順・任意）
- 現時点で未着手の候補なし（追加要望に応じて拡張）

## テスト
- `tests/bwv66_6.musicxml`: J.S.バッハ コラール(パブリックドメイン、4声体10小節)
- `tests/bwv66_with_clarinet.musicxml`: 上記にBbクラリネットを付与し移調検出を発火させた版
- `tests/bwv66_with_rehearsal.musicxml`: 上記にリハーサルマーク A(m.1)・B(m.4) を付与し
  拡張機能[6]の正常系を発火させた版。WARN系(空内容・重複)は含めず、異常系は
  別途インメモリ注入テストで担保する。
- 検証[8]の正常系は上記固定フィクスチャ(全パート同一小節数)で確認。小節数不一致や
  単一パートの異常系は固定譜を新規作成せず、インメモリ注入で担保する。
- いずれも著作権フリー。市販譜(Disney等)はテストに使用しないこと。

## 環境（殿のルール準拠）
- テスト環境: `/Users/takashioikawa/local_test_env/score-reader/`
- バックアップ: `/Users/takashioikawa/BakaUpArea/score-reader/`
- リポジトリ名 `score-reader` は仮称。確定後に置換すること。

## 既知の限界
- OMRが誤読したMusicXMLは「正しい入力」として処理される。本ツールは
  音価・拍子・移調の整合性は検査できるが、音高そのものの正誤は判定できない。
- 最終的な正確性は元譜との目視照合で担保する。
