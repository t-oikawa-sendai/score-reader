# Request Definition（要求定義）

<!-- Document Info（文書情報） -->
| Item（項目） | Value（値） |
|---|---|
| Document ID（文書ID） | REQ-001 |
| Version（バージョン） | 0.3.4 |
| Status（ステータス） | Draft |
| Created Date（作成日） | 2026-06-09 |
| Last Updated（最終更新日） | 2026-09-06 |
| Owner（管理者） | Takashi Oikawa |
| Related Documents（関連文書） | README.md / 02_REQUIREMENTS_DEFINITION.md / docs/reviews/2026-09-06_SCORE_READER_DESIGN_REVIEW.md / docs/reviews/2026-09-06_SCORE_READER_CROSS_DOCUMENT_REVIEW.md |

---

## Table of Contents（目次）

1. [Purpose（目的）](#1-purpose目的)
2. [Scope（対象範囲）](#2-scope対象範囲)
3. [Out of Scope（対象外範囲）](#3-out-of-scope対象外範囲)
4. [Assumptions（前提条件）](#4-assumptions前提条件)
5. [Definition Details（定義内容）](#5-definition-details定義内容)
6. [Open Issues（未決事項）](#6-open-issues未決事項)
7. [Handoff to Detail Design（詳細設計への引き継ぎ）](#7-handoff-to-detail-design詳細設計への引き継ぎ)
8. [Change History（変更履歴）](#8-change-history変更履歴)

---

## 1. Purpose（目的）

本文書は、score-reader の背景・課題・要求・成功基準を定義し、要件定義・設計フェーズの前提とすることを目的とする。全体方針は `README.md` を参照する。

開発目的は、現行技術では自動化できない楽譜データ化の問題に対して、次の境界を明示し、その設計判断を将来の開発者・保守者へ引き継ぐことである。

- 機械的に検出可能な領域
- 人間確認が必要な領域
- 現技術では自動化困難な領域

確認対象の絞り込みは目的であり、人間確認をゼロにすることではない。分類の詳細は §5.5 を参照する。

開発段階の分類は `README.md` §1 Development Stage Classification を参照する。現行プロトタイプ `verify_score.py` は単一 MusicXML 構造検査の検証実装であり、凍結中である。複数 MusicXML 比較は本実装確定スコープである（未実装。未確定ではない）。

本文書の対象読者は、設計者・開発者・プロジェクトオーナーである。

---

## 2. Scope（対象範囲）

本文書がカバーする対象を以下に示す。

| 対象 | 内容 |
|---|---|
| 開発段階 | 現行プロトタイプ（本文書の主対象）と、完成版MusicXML作成支援システムの本実装確定スコープ。全体整理は `README.md` §1 を参照 |
| システム | score-reader（OMR 出力 MusicXML 検査支援ツール） |
| フェーズ | プロトタイプ・技術検証フェーズ（現行実装）。本実装は確定スコープとして文書化するが、本フェーズでは実装しない |
| 現行プロトタイプ | 単一 MusicXML ファイルの内部整合性検査（凍結） |
| 本実装確定スコープ | 複数 MusicXML 入力、正規化、比較可能性判定、差分比較、差分可視化、比較レポート生成。未実装であり、未確定ではない |
| 対象ファイル（プロトタイプ） | 単一 MusicXML ファイルの内部整合性検査 |
| 対象ユーザー | OMR 出力を受け取り、楽譜データ化作業を行う担当者 |
| 検査対象 | OMR が生成した MusicXML の構造的異常・要確認箇所の抽出 |
| 利用OMRサービス | Newzik（現行運用）、その他 OMR サービス・ツール全般 |

---

## 3. Out of Scope（対象外範囲）

以下は本プロジェクトのスコープ外とする。自動修正、自動統合、Web UI は、現時点で定義する本実装段階には含めず、将来検討事項として扱う。原本 PDF 横並び画面は本リポジトリの対象外であり、本リポジトリでは設計・実装方針を定義しない。人間が MuseScore 等を使用して原本 PDF と目視照合する既存運用は維持する（`README.md` §1 Development Stage Classification 参照）。

| 対象外項目 | 理由 |
|---|---|
| MusicXML の完全自動正誤判定 | 現技術では音高・音価・声部の正誤を機械的に確定できない |
| 原本 PDF との内容一致確認 | MusicXML だけでは原本 PDF の内容を復元・比較できない |
| 原本 PDF 横並び画面 | 本リポジトリの対象外。本リポジトリでは設計・実装方針を定義しない。人間が MuseScore 等を使用して原本 PDF と目視照合する既存運用は維持する |
| 完成版アプリケーションの本フェーズでの実装 | 現行作業はプロトタイプ検証段階に限定する。本実装確定スコープは文書化するが、本フェーズでは実装しない |
| 楽譜編集・修正機能 | MuseScore 等の既存ツールが担当する領域である |
| 音声再生・楽譜表示機能 | 本ツールの目的は検査結果の出力であり、再生・表示は対象外 |
| 複数 MusicXML の自動マージ・統合 | 自動修正、自動統合は将来検討事項。複数 MusicXML 比較自体は本実装確定スコープであり、比較結果の採用は人間判断を要する |
| solacom-main 等、他システムとの連携 | score-reader は独立したシステムであり、他システムと無関係 |

---

## 4. Assumptions（前提条件）

本文書の内容が成立するために必要な前提を以下に示す。

**OMR・入力に関する前提**

- ユーザーは外部 OMR サービス（Newzik 等）により原本 PDF から MusicXML を取得済みであること
- 入力 MusicXML は `music21.converter.parse` が読み込める形式であること
- 現行プロトタイプの 1 回の検査対象は単一 MusicXML ファイルであること。本実装確定スコープは複数 MusicXML 入力を含む

**技術的前提**

- 現技術では、楽譜 PDF から MusicXML への**完全自動化は困難**であること
- OMR が生成した MusicXML には、音高・音価・声部・記号に誤認識・欠落が混入しうること
- 複数 OMR の結果が一致していても、同じ誤認識を出している可能性があること
- MusicXML の内部整合性が正常でも、原本 PDF と内容が異なる可能性があること

**Prototype の位置づけ**

- `verify_score.py`（`prototype/src/`）は技術検証用プロトタイプであり、**正式完成版ではない**
- 現段階のプロトタイプは単一 MusicXML の内部整合性検査に限定する
- プロトタイプの検証結果が、本設計の要求・要件定義に反映される
- 現行プロトタイプは凍結中である。設計文書の更新・検証は継続する

**人間確認に関する前提**

- score-reader の目的は**人間確認をゼロにすることではなく、確認対象を絞り込み確認作業を省力化すること**である
- 最終的な正確性の担保は原本 PDF との人間による目視照合が必要であること
- score-reader が警告・異常を検出しない場合でも、完全無欠を保証しないこと

**著作権・情報セキュリティに関する前提**

- テスト素材の利用可否は、「楽曲がパブリックドメインであること」だけでは完結しない
- 確認対象は次を分離する: 楽曲・作曲物の権利、MusicXML エンコーディングの権利、加工・再配布条件
- 著作権保護された譜面を公開リポジトリへ登録しないこと
- 外部 OMR サービスへ譜面を送信する際は、利用規約と著作権条件を事前確認すること
- 現在公開中のテスト素材は権利確認中であり、公開可否は法的に未確定である
- 公開可否の最終判断責任者は Takashi Oikawa である。公開判断の固定期限は設けない
- 権利判断が完了するまでの暫定措置は、権利確認中・公開可否未確定であることを明示した上での公開継続である
- 未解決のまま残す事項は、MusicXML エンコーディングの個別利用条件、再配布可否の法的確定、準拠法域である（`03_DATA_AND_SECURITY_DESIGN.md` TBD-005）

---

## 5. Definition Details（定義内容）

### 5.1 Background and Purpose（背景・課題・目的）

#### 背景

楽譜 PDF のデータ化（MusicXML 化）作業では、OMR（Optical Music Recognition）サービスを利用して楽譜 PDF を MusicXML に変換し、MuseScore 等で開いて人間が確認・修正する運用が行われている。

現行運用フロー:

```
原本 PDF → OMR サービス（例: Newzik） → MusicXML → MuseScore → 人手で確認・修正
```

#### 課題

現時点の OMR サービスは精度に大きな課題が残っており、OMR 出力 MusicXML をそのまま完成版として扱えない。

**OMR 精度の参考値（Newzik、利用者ヒアリングによる体感値）**

| 対象 | 精度参考値 |
|---|---:|
| 簡単な楽譜 | 約 95% |
| 難しい楽譜の音符の高さ・長さ | 約 70% |
| テキスト指示・臨時記号・多声部など | 30% 未満の場合あり |

上記は公式保証値ではなく、利用者ヒアリングによる参考値である。

**誤認識・欠落が発生し得る主な要素**

音符（音高・音価）、臨時記号、休符、複数小節休符、小節番号、拍子・調号、テンポ、強弱記号、リハーサルマーク、タイ・スラー、連符・装飾音符、多声部記譜、打楽器譜 など。

**完全自動化が困難な技術要因**

| 要因 | 内容 |
|---|---|
| 音高判定の不確実性 | 加線・符頭位置・調号・臨時記号・オクターブ違いにより確定できない |
| 音価・リズム判定の不確実性 | 符尾・連桁・付点・連符・休符の誤認識により小節内拍数が崩れうる |
| 声部分離の不安定性 | 同一譜表内複数声部の上声・下声・休符補完がツール間で異なる |
| 和音構成音の欠落・過検出 | 重なった符頭・内声部の解釈により欠落や余分な音が発生しうる |
| タイ・スラー・記号類の解釈差 | 見た目が近く、開始・終了位置・小節またぎの扱いが不安定 |
| 小節対応ずれの連鎖 | 1箇所の小節ずれが以降の比較全体に影響する可能性がある |
| MusicXML 表現差 | 同じ楽譜内容でも OMR ツールごとに XML 表現が異なる |
| 複数 OMR 間の同一誤認識 | 複数結果が一致しても同じ誤認識を出している可能性がある |

**作業負荷の現状**

難しい楽譜では、OMR 出力の部分修正では済まず、MuseScore 上で音符を一つずつ手入力し直す場合があり、1 楽譜のデータ化に約 50 時間を要するケースがある（利用者ヒアリング）。

#### 人間確認が必要な理由

MusicXML 単体では確定できない問題が多数存在するため、人間による原本 PDF との照合は不可欠である。

| 問題 | 理由 |
|---|---|
| どの OMR 結果が正しいか | 原本 PDF を見ないと確定できない |
| 欠落した音符の正解 | MusicXML だけでは復元不能 |
| タイ・スラーの正しい接続 | 表現意図を含むため人間による原本照合が必要 |
| 連符・装飾音符の正解 | 原本照合が必要 |
| 複数 OMR 一致時の正しさ | 同じ誤認識が起きる可能性がある |

#### 目的

開発目的の全体は §1 を参照する。score-reader は、次を分離して扱う。

1. **機械的に検出可能な領域**: 構造的異常・要確認箇所を自動抽出する（拍子不一致、移調情報欠落、パート間小節数不一致 など）
2. **人間確認が必要な領域**: 確認範囲を絞り込み、原本 PDF との照合を省力化する
3. **現技術では自動化困難な領域**: 機械では確定せず、自動化の対象にしない

この設計判断を将来の開発者・保守者へ引き継ぐ。人間確認をゼロにすることは目的ではない。成功基準上の分類は §5.5（SC / HC / ND）を参照する。

#### 解決方針（Solution Approach）

完成版MusicXML作成支援システムに向け、複数 OMR 結果の比較、MusicXML 内部構造検査、比較不能箇所の明示を組み合わせ、人間が原本 PDF と照合すべき箇所を絞り込む。推測による自動補完や入力ファイルの上書きは行わない。

### 5.2 Stakeholders（ステークホルダー一覧と関心事）

| Stakeholder（ステークホルダー） | Interests and Requests（関心事・要求） |
|---|---|
| 楽譜データ化作業者 | OMR 出力の怪しい箇所を素早く特定し、確認作業を効率化したい |
| プロジェクトオーナー / 設計者 | 現技術の限界を踏まえた現実的な支援ツールを設計・検証したい |
| 将来の利用者・保守者 | 設計意図・Prototype の位置づけ・限界を正確に把握したい |

### 5.3 User Stories and Use Cases（ユーザーストーリー・ユースケース概要）

詳細な機能要件は `02_REQUIREMENTS_DEFINITION.md` に記載する。

| ID | User Story / Use Case（ユーザーストーリー / ユースケース） |
|---|---|
| US-001 | 作業者として、OMR が生成した MusicXML を score-reader に渡すことで、拍子不一致・移調情報欠落・パート間小節数不一致などの構造的異常を自動で一覧取得したい。確認箇所を絞り込んで人手作業を効率化するため。 |
| US-002 | 作業者として、検査結果を人間可読テキスト形式または JSON 形式で出力したい。MuseScore での修正作業中に参照したり、後工程ツールに渡したりするため。 |
| US-003 | 作業者として、score-reader が「警告なし」を出した場合でも、それが完全無欠を保証するものではないことを明示的に知りたい。誤った安心感を持たずに原本照合を行うため。 |
| US-004 | 設計者として、複数の OMR サービスで同一 PDF を変換し、各出力の構造的特徴を比較した上で、人間が重点確認すべき箇所を絞り込みたい。複数 OMR が一致する箇所を暫定的に低優先度として扱うことで確認作業を効率化するため。 |
| US-005 | 設計者として、「自動化できること」「人間確認が必要なこと」「現技術では困難なこと」を明確に定義した設計書を残したい。将来の保守者・利用者が設計意図を誤解なく引き継ぐため。 |

### 5.4 Constraints（制約条件）

| Type（種別） | Constraint（制約内容） |
|---|---|
| Budget（予算） | 本文書では対象外。理由: 教育・研究目的の個人プロジェクトであり予算制約は現時点で定義しない |
| Deadline（期限） | 本文書では対象外。理由: プロトタイプ・検証フェーズのため期限は設定しない |
| Technical（技術）: 入力 | 現行プロトタイプの入力は単一 MusicXML ファイルのみ。本実装確定スコープは複数 MusicXML 入力を含む |
| Technical（技術）: ライブラリ | `music21 10.3.0` のみを依存ライブラリとする（`prototype/requirements.txt` 参照） |
| Technical（技術）: 非破壊 | 入力 MusicXML を読み取るのみで、上書き・更新・削除を行わない |
| Technical（技術）: 出力 | 検査結果は標準出力（テキストまたは JSON）への出力に限る。派生 MusicXML は出力しない |
| Technical（技術）: 限界 | 音高の正誤・原本 PDF との一致・どのパートが正しいか等は判定しない（詳細は §4 参照） |
| Regulatory（法規）: 著作権 | 著作権保護された譜面を公開リポジトリへ登録しない。テスト素材は楽曲の権利、MusicXML エンコーディングの権利、加工・再配布条件を分けて確認する |
| Regulatory（法規）: 外部サービス | 外部 OMR サービスへ譜面を送信する際は利用規約と著作権条件を事前確認する |
| Regulatory（法規）: 秘密情報 | API キー・トークン・秘密鍵・個人情報をソース・ログ・Git 管理対象へ含めない |

### 5.5 Success Criteria and Acceptance Conditions（成功基準・受け入れ条件）

テスト工程での検証基準は `06_OPERATION_AND_HANDOFF.md` に記載する。以下はビジネス観点・利用者観点での成功基準である。

**自動化できること（score-reader が機械的に検出できること）**

| ID | Success Criteria / Acceptance Condition（成功基準・受け入れ条件） |
|---|---|
| SC-001 | MusicXML のパース可否を判定し、パース失敗時は `[FATAL]` を出力して終了コード 1 で終了できる |
| SC-002 | 各パートの移調楽器情報の有無と記音→実音の半音差を報告できる |
| SC-003 | 拍子記号に対する小節内音価合計の不一致（`[ANOMALY]`）および拍子未確定小節（`[WARN]`）を検出できる |
| SC-004 | 調号・拍子・テンポ変化イベントを小節番号付きで列挙できる |
| SC-005 | リハーサルマークの小節対応・空内容・重複を検出できる |
| SC-006 | 和音構成音数の分布とパート内主流音数からの相違（`[WARN]`）を報告できる |
| SC-007 | 複数パート間の小節数不一致（`[WARN]`）を検出できる |
| SC-008 | 検査結果を人間可読テキスト形式および JSON 形式で出力できる |
| SC-009 | 検査 [5] として、スコア内の Unpitched（無音高）件数を報告できる |

**人間確認が必要なこと（score-reader が判定しないこと）**

| ID | 人間確認が必要な理由 |
|---|---|
| HC-001 | 音高の正誤（符頭位置・加線・臨時記号・オクターブ違い）は MusicXML のみでは確定できない |
| HC-002 | 和音構成音数が原本譜と一致するかは人間が原本 PDF と照合して判断する |
| HC-003 | パート間小節数不一致時に、どのパートが正しいかは人間が判断する |
| HC-004 | 複数 OMR の結果が一致していても、原本 PDF との照合なしに正解と断定してはならない |
| HC-005 | `[WARN]` / `[ANOMALY]` が 0 件でも、score-reader は完全無欠を保証しない |

**現技術では困難なこと（現フェーズのスコープ外）**

| ID | 現技術では困難な理由 |
|---|---|
| ND-001 | タイ・スラー・連符・装飾音の正誤判定は構造解釈に人間の音楽的判断が必要 |
| ND-002 | 多声部・打楽器特殊記譜の完全な構造比較は現時点の OMR 精度では不安定 |
| ND-003 | 複数 OMR 出力の自動マージ・統合による正解 MusicXML の自動生成 |

**削減効果について**

削減効果は未実測であり、現時点では定量的な成功基準としない。実測設計・測定方法は将来検討事項の検討課題とする（`01_REQUEST_DEFINITION.md` TBD-003）。

---

## 6. Open Issues（未決事項）

| ID | Open Issue（未決事項） | Owner（担当者） | Due Date（期限） | Status（ステータス） |
|---|---|---|---|---|
| TBD-002 | Prototype（verify_score.py）を正式版へ移行する基準・条件を定義するか。現設計はプロトタイプ検証フェーズに限定している。 | Takashi Oikawa | 未定 | Open |
| TBD-003 | 削減効果の実測設計・測定方法（測定対象・条件・手順・記録方式）を定義するか。現フェーズでは削減効果を定量的な成功基準としない。 | Takashi Oikawa | 未定 | Open |
| TBD-004 | Newzik 以外の OMR サービス（Audiveris、ACE Studio 等）との比較検証を本プロジェクトのスコープに含めるか。 | Takashi Oikawa | 未定 | Open |

テスト素材のうち未解決の権利事項は `03_DATA_AND_SECURITY_DESIGN.md` TBD-005 を参照する。最終判断責任者・暫定公開継続は §4 で確定済みである。公開判断の固定期限は設けない。技術的由来は `05_ARCHITECTURE_DESIGN.md` TBD-005 を参照する。

### Resolved Issues（解決済み事項）

| ID | Resolved Issue（解決済み事項） | Resolution（解決内容） | Status（ステータス） |
|---|---|---|---|
| TBD-001 | 複数 MusicXML 比較機能を将来検討事項で実装対象とするか。 | 完成版MusicXML作成支援システムの本実装確定スコープとする。対象は複数 MusicXML 入力、正規化、比較可能性判定、差分比較、差分可視化、比較レポート生成。現行プロトタイプは単一 MusicXML 構造検査のまま凍結する。自動修正、自動統合、Web UI は将来検討事項のままとする。原本 PDF 横並び表示は本リポジトリの対象外であり、本リポジトリでは設計・実装方針を定義しない。 | Resolved |

---

## 7. Handoff to Detail Design（詳細設計への引き継ぎ）

要件定義・アーキテクチャ設計フェーズへ引き継ぐ、本要求定義での主要な設計意図を示す。

1. **「推測しない」設計原則の継承**: 検査結果は断定せず、読めない・確定できない箇所を `[WARN]` / `[ANOMALY]` として列挙する原則を、正式版設計でも継続すること（`02_REQUIREMENTS_DEFINITION.md`・`05_ARCHITECTURE_DESIGN.md` の制約参照）。

2. **「自動化できること」と「人間確認が必要なこと」の境界を保持**: §5.5 の SC / HC / ND 分類を要件定義（`02_REQUIREMENTS_DEFINITION.md`）の機能要件・非機能要件に反映すること。この境界を曖昧にしないこと。

3. **Prototype の記録を参照源とすること**: `prototype/src/verify_score.py` の検査項目 [1]〜[8] および出力仕様は、要件定義・アーキテクチャ設計の出発点として参照すること。ただし、正式版では仕様変更・拡張が生じうるため、プロトタイプ実装との差分を明示的に管理すること。現行プロトタイプは凍結中である。

4. **完全自動化を約束しないこと**: 成功基準（§5.5）の「現技術では困難なこと」は、要件定義・UI 設計・運用設計においても利用者への誤解防止として一貫して扱うこと。

5. **本実装確定スコープを未確定と扱わないこと**: 複数 MusicXML 入力、正規化、比較可能性判定、差分比較、差分可視化、比較レポート生成は本実装の確定スコープである。現行プロトタイプの単一検査と混同しない。自動修正、自動統合、Web UI は将来検討事項のままとする。原本 PDF 横並び表示は本リポジトリの対象外であり、本リポジトリでは設計・実装方針を定義しない。人間が MuseScore 等を使用して原本 PDF と目視照合する既存運用は維持する。

---

## 8. Change History（変更履歴）

| Version（バージョン） | Date（日付） | Changes（変更内容） | Author（変更者） |
|---|---|---|---|
| 0.1 | 2026-06-09 | 初版作成 | Takashi Oikawa |
| 0.2 | 2026-06-28 | 正本記入・§3 将来検討事項表記・開発段階分類表記統一 | Takashi Oikawa |
| 0.3 | 2026-09-06 | 設計レビュー反映。テスト素材条件の分離、SC-009 追加、TBD 参照の文書名付き化 | Takashi Oikawa |
| 0.3.1 | 2026-09-06 | 設計レビュー差し戻し反映。TBD-001 を本実装確定スコープとして解消。テスト素材の暫定公開継続・最終判断責任者・判断期限を記録 | Takashi Oikawa |
| 0.3.2 | 2026-09-06 | 文書表現の整理。開発目的として機械と人間の役割境界を明示。仕様の追加・変更は行っていない | Takashi Oikawa |
| 0.3.3 | 2026-09-06 | 横断レビュー反映。公開判断の固定期限を廃止。SC-009 を件数報告に限定 | Takashi Oikawa |
| 0.3.4 | 2026-09-06 | 原本 PDF 横並び画面を将来検討事項から外し、本リポジトリでは設計・実装方針を定義しない対象へ変更。人間が MuseScore 等で原本 PDF と目視照合する既存運用は維持する | Takashi Oikawa |
