# Requirements Definition（要件定義）

<!-- Document Info（文書情報） -->
| Item（項目） | Value（値） |
|---|---|
| Document ID（文書ID） | REQS-001 |
| Version（バージョン） | 0.2 |
| Status（ステータス） | Draft |
| Created Date（作成日） | 2026-06-09 |
| Last Updated（最終更新日） | 2026-06-28 |
| Owner（管理者） | Takashi Oikawa |
| Related Documents（関連文書） | README.md / 01_REQUEST_DEFINITION.md / 03_DATA_AND_SECURITY_DESIGN.md / 04_UI_AND_FLOW_DESIGN.md / 05_ARCHITECTURE_DESIGN.md |

---

## Table of Contents（目次）

1. [Purpose（目的）](#1-purpose目的)
2. [Scope（対象範囲）](#2-scope対象範囲)
3. [Out of Scope（対象外範囲）](#3-out-of-scope対象外範囲)
4. [Assumptions（前提条件）](#4-assumptions前提条件)
5. [Definition Details（定義内容）](#5-definition-details定義内容)
6. [Open Issues（未決事項）](#6-open-issues未決事項)
7. [Handoff to Detail Design（詳細設計への引き継ぎ）](#7-handoff-to-detail-design詳細設計への引き継ぎ)
8. [Change History（変更履歴）](#8-change-history変更履歴)

---

## 1. Purpose（目的）

本設計書群は、完成版 MusicXML 作成支援システムへ到達するための検証結果・設計判断・未確定事項を整理したプロトタイプ段階の設計ガイドラインである。本文書は**プロトタイプ検証段階**における `verify_score.py`（単一 MusicXML 構造検査の検証実装）が満たすべき機能要件・非機能要件を定義し、実装・検証フェーズの基準とすることを目的とする。

将来本実装候補（複数 MusicXML 比較、差分可視化、比較レポート生成）の要件は、本文書の Open Issues および `01_REQUEST_DEFINITION.md` で管理する。本文書の前提となる背景・課題・要求は `01_REQUEST_DEFINITION.md` に記載する。本文書の対象読者は、設計者・実装者・プロジェクトオーナーである。

---

## 2. Scope（対象範囲）

| 対象 | 内容 |
|---|---|
| 開発段階 | **プロトタイプ検証段階**（`README.md` §1 Development Stage Classification 参照） |
| システム | score-reader（OMR 出力 MusicXML 検査支援 CLI ツール） |
| 対象ファイル | 単一 MusicXML ファイルの内部整合性検査 |
| インターフェース | CLI（コマンドライン引数による入力ファイル指定・出力形式選択） |
| 出力先 | 標準出力のみ（テキスト形式 / JSON 形式） |

---

## 3. Out of Scope（対象外範囲）

以下は本文書の要件定義対象外とする。

| 対象外項目 | 理由 |
|---|---|
| 音高・音価の正誤自動判定 | 現技術では確定できない。人間による原本 PDF 照合が必要 |
| 原本 PDF との内容一致確認 | MusicXML のみでは原本内容を復元・比較できない |
| 楽譜編集・修正・再出力機能 | score-reader は検査結果の出力に専念する非破壊ツールである |
| 複数 MusicXML の自動差分比較・マージ | プロトタイプ検証段階のスコープ外。将来検討事項の検討課題（TBD-001） |
| Web UI・GUI | プロトタイプは CLI ツールとして実装する |
| 外部 API の公開・提供 | プロトタイプ検証段階では外部 API を持たない（TBD-001） |
| 音声再生・楽譜表示 | 検査結果の出力以外の UI 機能は対象外 |
| 完成版アプリケーションの実装 | 本文書はプロトタイプ・検証フェーズの要件定義に限定する |

---

## 4. Assumptions（前提条件）

本文書の内容が成立するために必要な前提は `01_REQUEST_DEFINITION.md` §4 を継承する。以下は要件定義フェーズで追加する前提を示す。

- 入力 MusicXML は `music21.converter.parse` が読み込める形式であること
- 依存ライブラリは `music21 10.3.0` のみとする（`prototype/requirements.txt` 参照）
- 機能要件 FR-001〜FR-012 はプロトタイプ（`verify_score.py`）の現仕様に基づく
- `[WARN]` / `[ANOMALY]` が 0 件であっても、MusicXML の完全無欠を保証しないことを要件として扱う
- 本文書の Created Date は Document Info（2026-06-09）を参照する

---

## 5. Definition Details（定義内容）

### 5.1 Functional Requirements（機能要件一覧）

#### 基本機能

| ID | Feature Name（機能名） | Priority（優先度） | Details（詳細） |
|---|---|---|---|
| FR-001 | MusicXML パース | High | コマンドライン第 1 引数で指定されたファイルを `music21.converter.parse` で読み込む。パース失敗時は `[FATAL]` を標準出力に出力し、終了コード 1 で終了する |
| FR-010 | 人間可読テキスト出力 | High | デフォルト動作として、検査 [1]〜[8] の結果を人間可読テキスト形式で標準出力に出力する。出力順序: [1]→[2]→[3]→[4]→[6]→[7]→[8]→[5] |
| FR-011 | JSON 出力 | High | `--json` フラグ指定時、検査 [1]〜[8] の結果を構造化 JSON 形式で標準出力に出力する |
| FR-012 | 終了コード管理 | High | パース成功時: 終了コード 0。パース失敗時: 終了コード 1 |

#### 検査機能（[1]〜[8]）

| ID | Feature Name（機能名） | Priority（優先度） | Details（詳細） |
|---|---|---|---|
| FR-002 | [1] 移調楽器チェック | High | 各パートの移調楽器情報（有無・半音差・方向）を報告する。移調楽器が 1 件も検出されない場合は、OMR が移調情報を欠落させている可能性を注記する |
| FR-003 | [2] 小節長の検算 | High | 各パートの各小節について、拍子記号に対する音価合計（四分音符単位）を検算する。拍子未確定: `[WARN]`、期待と不一致: `[ANOMALY]`、アウフタクト許容条件（小節番号が 0 または 1、かつ実測音価が期待音価未満）: 許容し異常としない。ただしこれは簡易判定であり、すべての弱起を正しく判定する保証ではない |
| FR-004 | [3] 調号の列挙 | Medium | 第 1 パート（`score.parts[0]`）から調号を列挙し、出現小節番号・シャープ/フラット数を出力する |
| FR-005 | [4] テンポ/拍子変化イベントの列挙 | Medium | 第 1 パートから拍子記号とテンポ（メトロノームマーク）の変化を小節番号順に列挙する |
| FR-006 | [5] 未確定・要確認要素の報告 | Medium | スコア全体の `Unpitched`（無音高）要素の件数を報告する。件数が 0 でも完全無欠を保証しない旨を注記する |
| FR-007 | [6] リハーサルマーク対応表 | Medium | 全パートからリハーサルマークを収集し、小節番号との対応を出力する。重複排除は「小節番号 + マーク内容」の組で正規化する（同一内容でも小節番号が異なる場合は別マークとして扱う）。検出ゼロ・小節番号未確定・内容空・同一小節に複数マーク: 各々 `[WARN]` |
| FR-008 | [7] 和音音数チェック | Medium | 各パートの和音（`chord.Chord`）について構成音数を集計・報告する。主流音数は「パート内の和音構成音数の最頻値」と定義する。単音が和音として記譜: `[INFO]`、構成音数が主流音数と異なる和音: `[WARN]`、和音 0 件: 取りこぼし可能性を注記。正誤判定ではなく確認対象の抽出であり、音数の正誤は断定しない |
| FR-009 | [8] パート間小節数整合チェック | High | 各パートの小節数を比較する。パートが 1 つのみ: `[INFO]`、全パート一致: 一致結果を出力、不一致: `[WARN]`。どのパートが正しいか・どの小節が欠落したかは断定しない |

#### 出力レベル定義

| 出力レベル | 意味 |
|---|---|
| `[FATAL]` | パース失敗。処理を中断し終了コード 1 で終了 |
| `[ANOMALY]` | 小節長と拍子の不一致など、構造上の異常 |
| `[WARN]` | 要確認の疑い（拍子未確定・リハーサルマーク異常・和音音数の相違・パート間小節数不一致 など） |
| `[INFO]` | 参考情報（単音が和音として記譜・無音高要素の件数 など） |

#### JSON 出力フィールド定義（FR-011 補足）

| フィールド | 内容 |
|---|---|
| `parts` | パート数 |
| `transposing_instruments` | [1] 移調楽器情報 |
| `measure_anomalies` | [2] 小節長異常（WARN / ANOMALY のみ） |
| `key_signatures` | [3] 調号 |
| `tempo_meter_events` | [4] 拍子・テンポ変化イベント |
| `rehearsal_marks` | [6] リハーサルマーク |
| `chord_voicing` | [7] 和音音数 |
| `unreadable` | [5] 未確定要素 |
| `part_measure_counts` | [8] パート間小節数整合 |
| `warnings` | 全セクションの WARN / ANOMALY を集約した配列 |
| `note` | 検査の限界に関する注記 |

### 5.2 Non-Functional Requirements（非機能要件）

セキュリティの設計仕様（著作権保護・情報管理の実装方針）は `03_DATA_AND_SECURITY_DESIGN.md` に記載する。

#### 基本非機能要件

| Type（種別） | Requirement（要件内容） |
|---|---|
| Performance（性能） | 一般的な楽譜（数十小節・数パート程度）の MusicXML に対して、実用上の待機時間（数秒以内）で検査結果を出力できること。性能の定量基準はプロトタイプ検証段階では規定しない（TBD-004） |
| Availability（可用性） | プロトタイプ・ローカル実行ツールのため、可用性の定量基準は規定しない |
| Security Requirement Level（セキュリティ要求レベル） | 低〜中。テスト素材はパブリックドメインに限定し、著作権保護楽譜・API キー・個人情報をリポジトリに含めない |
| Maintainability（保守性） | 検査項目の追加・変更が容易な構造を維持すること。依存ライブラリは `music21 10.3.0` のみとし、外部依存を最小化すること |

#### 非破壊要件

| 要件 | 内容 |
|---|---|
| 入力ファイル保護 | 入力 MusicXML ファイルを読み取るのみで、上書き・更新・削除を行わない |
| 派生ファイル出力禁止 | 修正済み MusicXML・派生 MusicXML を出力しない |
| 出力先制限 | 検査結果は標準出力（テキストまたは JSON）への出力に限る |

#### 設計原則要件（推測禁止・断定禁止）

| 要件 | 内容 |
|---|---|
| 推測禁止 | 読めない・断定できない箇所は黙殺せず `[WARN]` 等として列挙する |
| 断定禁止 | 「何音であるべきか」「どのパートが正しいか」等は推測・断定しない |
| 完全無欠の保証禁止 | `[WARN]` / `[ANOMALY]` が 0 件であっても、MusicXML の正確性・完全無欠を保証する表現・動作を禁止する |
| 完全自動化の約束禁止 | 出力結果が音高・原本一致の正確性を保証するような表現・動作を禁止する |

#### 人間確認要件

| 要件 | 内容 |
|---|---|
| 原本照合の必要性の明示 | 検査結果に「最終的な正確性は原本 PDF との目視照合で担保する必要がある」旨を含める |
| 検査限界の明示 | 音高の正誤・和音の正確性・パート間不一致の正解・タイ/スラー/連符等は判定しない旨を出力する |
| 複数 OMR 一致の誤解防止 | 本ツール単体では複数 OMR 間の一致を正解の根拠として扱わない |

#### 検査できないことの要件定義（Out of Scope 要件）

以下は score-reader が判定・保証しないことを要件として明示する。

| 判定しないこと | 理由 |
|---|---|
| 音高の正誤（符頭位置・加線・臨時記号・オクターブ違い） | MusicXML の構造検査では確定できない |
| 和音構成音数が原本譜と一致するかの断定 | 正解の定義が原本 PDF に依存する |
| パート間小節数不一致時にどのパートが正しいか | 原本 PDF を見ないと確定できない |
| どの小節が欠落・過剰認識されたかの特定 | MusicXML だけでは復元不能 |
| タイ・スラー・連符・装飾音の正誤 | 構造解釈に音楽的判断が必要 |
| 強弱記号・テキスト指示・アクセント等の欠落 | 検査対象外（プロトタイプ検証段階） |
| 複数 OMR 一致時の正しさの保証 | 同じ誤認識が起きる可能性がある |
| `[WARN]` / `[ANOMALY]` が 0 件の場合の完全無欠の保証 | 本ツールの検査限界によるもの |

### 5.3 Screen List（画面一覧）

本文書では対象外。理由: score-reader は CLI ツールであり、画面（UI）を持たない。入力・出力インターフェースの詳細（CLI 引数・出力形式・操作フロー）は `04_UI_AND_FLOW_DESIGN.md` に記載する。

### 5.4 API Overview（API一覧の概要）

本文書では対象外。理由: プロトタイプ検証段階では外部公開・内部利用 API を持たない。将来検討事項での API 化は `TBD-001`（§6）として管理する。

### 5.5 Data Overview（データ種別・件数規模の概要）

詳細なデータ設計は `03_DATA_AND_SECURITY_DESIGN.md` に記載する。

| Data Type（データ種別） | Estimated Volume（想定件数・規模） | Notes（備考） |
|---|---|---|
| 入力 MusicXML ファイル | 単一ファイル（1 実行 = 1 件） | `.musicxml` 等、`music21.converter.parse` が対応する形式 |
| 標準出力（テキスト形式） | 1 実行 = 1 出力 | 画面表示のみ。ファイル保存は利用者のリダイレクト操作に委ねる |
| 標準出力（JSON 形式） | 1 実行 = 1 出力 | `--json` フラグ指定時。ファイル保存は利用者に委ねる |
| テスト素材 MusicXML | プロトタイプ検証段階: 数件 | パブリックドメイン楽譜に限定。著作権保護楽譜はリポジトリに含めない |

### 5.6 Mapping to Request Definition（要求定義との対応マッピング）

`01_REQUEST_DEFINITION.md` のユーザーストーリー ID と機能要件 ID の対応を示す。

| User Story ID（ユーザーストーリー ID） | Functional Requirement ID（対応する機能要件 ID） |
|---|---|
| US-001（構造的異常の自動抽出） | FR-001, FR-002, FR-003, FR-004, FR-005, FR-006, FR-007, FR-008, FR-009 |
| US-002（人間可読 / JSON 出力） | FR-010, FR-011, FR-012 |
| US-003（警告なし＝正確でないことの明示） | NFR（完全無欠の保証禁止）、NFR（原本照合の必要性の明示） |
| US-004（複数 OMR 比較の前段として単一検査） | FR-001〜FR-009（単一 MusicXML 検査の基盤として機能） |
| US-005（設計意図の引き継ぎ） | NFR（設計原則要件）、§7 Handoff |

また `01_REQUEST_DEFINITION.md` の成功基準との対応を示す。

| 成功基準 ID | 対応する機能要件 / 非機能要件 |
|---|---|
| SC-001（パース判定） | FR-001, FR-012 |
| SC-002（移調楽器報告） | FR-002 |
| SC-003（小節長不一致検出） | FR-003 |
| SC-004（調号・拍子・テンポ列挙） | FR-004, FR-005 |
| SC-005（リハーサルマーク検出） | FR-007 |
| SC-006（和音音数分布報告） | FR-008 |
| SC-007（パート間小節数不一致検出） | FR-009 |
| SC-008（テキスト / JSON 出力） | FR-010, FR-011 |
| HC-001〜HC-005（人間確認が必要な項目） | NFR（検査できないことの要件定義） |

---

## 6. Open Issues（未決事項）

| ID | Open Issue（未決事項） | Owner（担当者） | Due Date（期限） | Status（ステータス） |
|---|---|---|---|---|
| TBD-001 | 複数 MusicXML 入力・差分比較機能を将来検討事項で要件化するか。プロトタイプは単一ファイルのみ。 | Takashi Oikawa | 未定 | Open |
| TBD-002 | FR-004（調号）・FR-005（テンポ/拍子）の参照対象を「第 1 パートのみ」から「全パート」に拡張するか。 | Takashi Oikawa | 未定 | Open |
| TBD-003 | FR-006（未確定要素）の検出対象を `Unpitched` 以外（スラー欠落・連符崩れ等）に拡張するか。 | Takashi Oikawa | 未定 | Open |
| TBD-004 | 性能要件（検査実行時間の上限）をプロトタイプ検証後に定量化するか。 | Takashi Oikawa | 未定 | Open |

---

## 7. Handoff to Detail Design（詳細設計への引き継ぎ）

アーキテクチャ設計（`05_ARCHITECTURE_DESIGN.md`）・実装フェーズへ引き継ぐ主要事項を示す。

1. **「推測しない」設計原則を実装に徹底すること**: 断定できない結果は `[WARN]` / `[INFO]` として列挙する。黙殺・自動補完・推測による埋め合わせを行わないこと（NFR §5.2 設計原則要件 参照）。

2. **FR-004・FR-005 の参照対象の制約を引き継ぐこと**: 調号・テンポ/拍子の列挙は「第 1 パート（`score.parts[0]`）のみ」の制約を `05_ARCHITECTURE_DESIGN.md` および実装コメントで明示すること。

3. **非破壊仕様を実装レベルで保持すること**: 入力ファイルの読み取り専用・派生ファイル出力禁止・標準出力限定は実装レベルで変更しないこと（NFR §5.2 非破壊要件 参照）。

4. **`[WARN]` / `[ANOMALY]` 0 件時の注記を出力に組み込むこと**: 警告 0 件であっても「完全無欠を保証しない」旨の注記を出力に含めること（FR-006、NFR §5.2 人間確認要件 参照）。

---

## 8. Change History（変更履歴）

| Version（バージョン） | Date（日付） | Changes（変更内容） | Author（変更者） |
|---|---|---|---|
| 0.1 | 2026-06-09 | 初版作成 | Takashi Oikawa |
| 0.2 | 2026-06-28 | 正本記入・開発段階分類表記統一 | Takashi Oikawa |
